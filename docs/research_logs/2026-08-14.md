# Research development log — 2026-08-14

## Step 20/21 tariff-counterfactual reporting

- Corrected the no-PV counterfactual to use each utility's explicitly
  configured retail import plan. The former implementation could select the
  first numeric tariff column, making the result depend on CSV column order.
- Tariff-result selection now requires either a matching plan token or a
  single unambiguous numeric candidate after applying the retail/NEM3 variant.
  Missing and ambiguous selections raise an error.
- Step 20's aggregate and per-county paths now share the same retail-plan bill
  selection contract and preserve electricity/gas bill components.
- Step 21's per-county comparison is a required output. Missing, duplicate, or
  non-reconciling county rows raise an error instead of being silently skipped.
- This is a reporting-only correction. It does not change the Step 9b
  co-optimization, dispatch, PV sizing, or battery sizing results.
